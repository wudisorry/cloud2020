server:
  port: 9001
spring:
  application:
    name: cloudalibaba-nacos-payment-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
#做监控
management:
  endpoints:
    web:
      exposure:
        include: '*'